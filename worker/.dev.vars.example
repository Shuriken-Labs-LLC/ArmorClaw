# Copy to .dev.vars for local `wrangler dev`. Real values live ONLY here and in
# `wrangler secret put`. Never commit the real .dev.vars file.
STRIPE_SECRET_KEY=sk_test_REPLACE_ME
STRIPE_WEBHOOK_SECRET=whsec_REPLACE_ME
